------------------------------------------------------------
---------------------------GENERAL--------------------------
------------------------------------------------------------

add 'how to install on readme'
add install.sh file
find a way to hide files on newtree without having to add '.' at the beginning of the file name

create a bash script that automatically takes all the used firefox profiles and 
takes its ID and add it on the script that symlinks to all firefox profiles the css configs.

create a global alias for `rm` to `rm -i` just in case i accidently delete a file

------------------------------------------------------------
----------------------------NVIM----------------------------
------------------------------------------------------------

refactor nvim dotfiles, only what i changed should be on the repo, not the whole lazynvim source

when typing j on the last line, take cursor to the last character
when typing k on the first line, take cursor to the first character
when typing l on the last character, take cursor to the first character of the next line
when typing h on the first character, take cursor to the last character of previous line
